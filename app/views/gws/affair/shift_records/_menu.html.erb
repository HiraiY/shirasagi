<nav class="nav-menu">
  <% if params[:action] =~ /index/ %>
    <%= link_to t('ss.links.back'), gws_affair_shift_calendars_path %>
    <% if @shift_calendar.allowed?(:edit, @cur_user, site: @cur_site) %>
      <%= link_to t("ss.links.download"), action: :download %>
      <%= link_to t("ss.links.import"), action: :import %>
    <% end %>
    <% if @shift_calendar.allowed?(:delete, @cur_user, site: @cur_site) %>
      <%= link_to t('ss.links.delete'), delete_gws_affair_shift_calendar_path(id: @shift_calendar) %>
    <% end %>
  <% elsif params[:action] =~ /import/ %>
    <%= link_to t('ss.links.back_to_index'), action: :index %>
  <% end %>
</nav>
