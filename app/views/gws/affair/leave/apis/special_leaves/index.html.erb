<%= jquery do %>
  SS_SearchUI.modal();
<% end %>

<% tabs = SS.config.gws.user["staff_category"] %>
<div class="search-ui-form">
  <%= form_for :s, url: { action: :index }, html: { method: "GET", class: :search } do |f| %>
    <dl class="see">
      <dt><%= @model.t :staff_category %></dt>
      <dd>
        <%= f.select :staff_category, tabs,
                     { selected: params[:s].try(:[], :staff_category), include_blank: true },
                     { class: "submit-on-change" } %>
      </dd>
      <dt><%= @model.t :name %></dt>
      <dd>
        <%= f.text_field :keyword, value: params[:s].try(:[], :keyword) %>
        <%= f.submit  t("ss.buttons.search"), class: :btn %>
      </dd>
    </dl>
  <% end %>
</div>

<table class="index">
  <thead class="list-head">
  <tr>
    <th class="name"><%= @model.t :code %></th>
    <th class="name"><%= @model.t :name %></th>
    <th class="name"><%= @model.t :staff_category %></th>
  </tr>
  </thead>
  <tbody class="items">
  <% @items.each do |item| %>
    <tr class="list-item" data-id="<%= item.id %>" data-name="<%= item.name %>">
      <td><%= item.code %></td>
      <td><%= link_to item.name, "#", class: "select-single-item" %></td>
      <td><%= item.staff_category %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @items if @items.try(:current_page) %>
